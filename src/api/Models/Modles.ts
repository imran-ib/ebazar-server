import { objectType } from "@nexus/schema";

export const User = objectType({
  name: "User",
  definition(t) {
 
  },
});


export const Like = objectType({
  name: "Like",
  definition(t) {
  
  },
});

